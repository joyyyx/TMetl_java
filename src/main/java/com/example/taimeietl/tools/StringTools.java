package com.example.taimeietl.tools;

public class StringTools {
    public  static boolean isEmpty(String s){
        if (s.isEmpty()||s==null)
            return true;
        else
            return false;
    }
}
